# Track large video/audio files with Git LFS
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

# Preview clips stay in regular git so GitHub renders them inline in README
**/preview.mp4 filter= diff= merge=
